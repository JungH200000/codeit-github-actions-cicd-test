#name: CI Workflow # 현재 workflow의 이름
#on: # workflow 실행 조건
#  push: # push가 발생하는 시점에
#    branches: [ "main" ] # main branch에서
#
#jobs: # 실행할 작업(job) 정의
#  build: # build라는 작업 설정
#    runs-on: ubuntu-latest # 실행 환경 설정(build라는 job이 이 환경에서 실행됨)
#
#    steps: # job 안에서 실행될 단계
#      - uses: actions/checkout@v3 # 현재 repo의 code를 checkout(가저옴)
#
#      - name: JDK 17 설정
#        uses: actions/setup-java@v3
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#
#      - name: 실행 권한 부여
#        run: chmod +x gradlew
#
#      - name: 빌드 및 테스트
#        run: ./gradlew clean build
#
#      - name: Dokcer Build 및 commit 해시태그 설정
#        run: |
#          IMAGE_TAG=${GITHUB_SHA::7}
#          docker build -t myapp:$IMAGE_TAG .